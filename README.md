# web-scraping
Web scraping using Beautiful Soup 

# Simple Web Scraper Project

This project scrapes quotes from a practice website using Python.

## Required Libraries
To run this script, the code uses these specific imports:
1. `from bs4 import BeautifulSoup`
2. `import requests`
3. `import bs4`

## Installation

1. **Install Python** if you haven't already (Python 3.7+ recommended).

2. **Install the libraries** using the following command:
   ```bash
   pip install requests beautifulsoup4
